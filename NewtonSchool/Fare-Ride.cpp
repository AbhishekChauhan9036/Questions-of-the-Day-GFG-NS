#include <bits/stdc++.h> // header file includes every Standard library
using namespace std;

int main() {
    int k1, k2;
    cin >> k1 >> k2;

    bool k3 = (k1 >= k2);
    cout << boolalpha << k3 << endl;

    return 0;
}
