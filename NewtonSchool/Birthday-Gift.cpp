#include <bits/stdc++.h> // header file includes every Standard library
using namespace std;

int main() {
    int k1, k2;
    cin >> k1 >> k2;
    cout << (k1 >= k2 ? "true" : "false") << endl;
    return 0;
}
